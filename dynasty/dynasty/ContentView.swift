import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("DYNASTY")
                .font(.system(size: 64, weight: .black))
                .tracking(8)
        }
    }
}

#Preview {
    ContentView()
}
